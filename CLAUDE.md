# プロジェクト: ラジオCM自動カットアプリ

## 現在地（2026-09-12 時点・最初に読むこと）

**目標が変わっている。** 「CMを検出してカットする」→「**番組全体の区切り（曲↔トーク、曲↔曲、番組↔CM）を見つける**」。
CMかどうかの最終判断はユーザーが画面で行う。詳細と根拠は [SEGMENTATION_SPEC.md](SEGMENTATION_SPEC.md)。

この転換の根拠は文献調査で得た数値にある。放送音声の音楽検出は**学習済みCNNでも正解率88.95%、
人間同士の一致でも94.78%**が上限（OpenBMAT / TISMIR、本プロジェクトで原典を直接確認）。
モデルを使わない方針で「自動で正しく切る」を目標に据えるのは原理的に誤りで、
**人が確認する前提で候補を出す**設計が正しい。

作業ブランチ: `feature/detection-tuning-2`（masterへは未マージ）。

- **UI**: ほぼ完成。波形の拡大縮小・ドラッグ移動・クリック再生・区切りの手動追加/結合まで実装済み
- **評価ハーネス**: 実装済み（`src/RadioCmCutter.Core/Evaluation/`）。これが**今後の作業の土台**
- **検出アルゴリズム**: 現行は Foote+Dixon による境界検出。[SEGMENTATION_SPEC.md](SEGMENTATION_SPEC.md) の
  再設計は**未着手**。次の着手は「時間ラグ行列による繰り返し拒否」と「解析を16kHzへ変更」

## 重要な前提

- **本プロジェクトディレクトリは検討完了後に削除される予定**（ユーザー言及: 2026-09-11）
- 本ファイルは別PC等での作業引き継ぎのため、決定事項・保留事項を含む情報を**すべて**記録する目的で作成している
- 詳細仕様は [SPEC.md](SPEC.md) にも記載（本ファイルと内容重複あり。本ファイルが最新の決定事項を優先）
- 2026-09-11時点: **仕様検討フェーズ。実装は未着手**

## プロジェクトの目的

ラジオ番組の録音ファイルからCM区間を自動検出してカットするWindows向けローカルアプリを作る。

### 元の要望（ユーザー原文ベース）

- zip解凍してexe実行で動くのが理想
- ライブラリ（DLLなど）の利用はOKとするが出来れば同梱したい（別PCで利用時に別途FFmpegインストールなどは出来れば避けたい）
- 外部APIなどネット経由のものは一切使わない
- ごく軽量のローカルLLMであればOKとするが、将来的にAndroidアプリ化も検討しているので使わないほうがいいかもしれない
- CM部分の検出アルゴリズムについてはアイデアなし、提案を求める
- 対応ファイル形式はmp3, aac, m4aは必須、他対応容易であれば追加（wavなど）
- カットして自動再結合するモードと、カットのみモード（動作確認も兼ねる）を実装
- 画面上で検出結果の確認ができるとなお良い（スキップせずそのままカットも可）（難がある場合は見送ってもよい）
- 1ファイル指定で実行と、ディレクトリ指定で連続実行が可能

## 決定事項

| 項目 | 決定内容 |
|---|---|
| 配布形態 | zip解凍→exe実行。インストーラー不要 |
| 外部通信 | 一切なし（API通信、アップデートチェック等も含めない） |
| 対応ファイル形式 | 必須: mp3, aac, m4a／追加候補: wav等（FFmpeg同梱前提なので追加コストは低い） |
| 動作モード | ①カット＋自動再結合モード ②カットのみモード（動作確認用） |
| 実行方式 | 単一ファイル指定 ／ ディレクトリ指定バッチ処理（両方実装） |
| CM検出アルゴリズム | **①繰り返しセグメント検出（メイン）＋②音量・ラウドネス境界検出（補助・境界精緻化用）** のハイブリッド方式。詳細は本ファイル「CM検出アルゴリズム」章、および[SPEC.md](SPEC.md) |
| 確認UI | 波形表示＋検出区間のハイライト表示。区間ごとにカット対象ON/OFF切替可能にしてから最終カット実行 |
| ローカルLLM/STT | **今回は導入しない**。将来、文字起こし等の追加機能も含めて再検討予定（下記「今後の検討事項」参照） |
| 実装言語・フレームワーク | **確定: C#/.NET 10 (LTS), WPF**。2026-09-11時点で.NET8はサポート終了間近（2026年11月）、.NET9はSTSで既にサポート終了のため、最新LTSの.NET10を採用。将来Android化する場合は .NET MAUI でコード資産を再利用しやすい点も理由の一つ |

## CM検出アルゴリズム（詳細）

### ①繰り返しセグメント検出（メイン）
- 同一CM音源は同一番組内や別日の録音間で繰り返し使用される傾向があるという前提
- 音声を一定間隔（0.5〜1秒程度）のチャンクに分割し、音響特徴量（MFCC等）を抽出
- チャンク間（ファイル内・ファイル間）の類似度を計算し、高い類似度で繰り返し出現する区間をCM候補としてマーキング
- ディレクトリ一括処理（同番組の複数日分）と相性が良く、処理対象が増えるほど精度が向上する

### ②音量・ラウドネス境界検出（補助）
- CM区間はコンプレッサーがかかり音量が均一で大きい傾向がある、という特徴を利用
- ①で検出した候補区間の開始・終了位置を、RMS/ラウドネスの変化点や無音区間を基準に精緻化する
- 単独でのCM/本編判別には使わず、境界のズレ補正用途に限定

### 見送り・将来検討
- ジングル・シグナル音のテンプレートマッチング（番組ごとの手動登録が必要なため今回は見送り）
- ローカルSTT/LLMによるキーワード検出（下記参照）

## 今後の検討事項（保留中）

1. **ローカルLLM/STTの将来的な導入**
   - 今回は不使用の方針で確定
   - 将来、文字起こし機能等の追加価値も含めて再検討予定（ユーザー: 2026-09-11「導入の際は文字起こしなど追加機能も出来そうなので、後日検討」）
   - 検討時の論点: CM特有フレーズ（「お問い合わせは」等）検出による精度向上 vs モデル同梱によるサイズ増加・Android移植時のSTT実行コスト

## 未着手の細部（実装フェーズで詰める）

- CM候補区間の最小長・最大長の閾値、繰り返し判定の類似度閾値などのパラメータ
- 誤検出時の学習・調整機能の有無（ユーザーによる手動補正結果を次回以降に活かすか）
- 出力ファイルの命名規則、出力先ディレクトリ構成
- 処理ログ・レポート（検出区間の時刻リストをテキスト等で出力するか）

## 開発環境

- **.NET 10 SDK**（Windows x64, SDK版）をインストール（[dotnet.microsoft.com/ja-jp/download](https://dotnet.microsoft.com/ja-jp/download)）。Runtimeのみではなく必ずSDKを選ぶ
- WPF利用のため追加ワークロードのインストールは不要（.NET SDKに標準で含まれる）
- エディタ: VSCode + 拡張機能「C# Dev Kit」（Microsoft公式）

## 実装状況（2026-09-11時点）

### プロジェクト構成
- `RadioCmCutter.slnx`（ソリューション、.slnx形式）
- `src/RadioCmCutter.Core`: 音声解析・検出・カットのロジック（UI非依存、将来のAndroid化時に再利用する想定）
- `src/RadioCmCutter.App`: WPFアプリ本体（net10.0-windows）
- `src/RadioCmCutter.Core.Tests`: xUnitテスト（Coreロジックのみ対象。`dotnet test`で実行可能）

### 実装済み（Core）
- `Ffmpeg/FfmpegLocator.cs`: ffmpeg.exe/ffprobe.exeの実体を`<実行ファイルの場所>/tools/ffmpeg/`から探す。無ければPATH上のものにフォールバック
- `Ffmpeg/AudioDecoder.cs`: ffmpegでモノラル8kHz PCMにデコード（解析用）、ffprobeで長さ取得
- `Ffmpeg/AudioCutter.cs`: 削除区間の補集合（残す区間）を計算し、ffmpegで①結合カット②個別書き出し（カットのみモード）を行う
- `Detection/Fft.cs`: 自作Radix-2 FFT（外部ライブラリ依存を避けるため自前実装）
- `Detection/FeatureExtractor.cs`: 0.5秒フレームごとに対数帯域エネルギー特徴（24バンド、L2正規化）を抽出
- `Detection/RepeatSegmentDetector.cs`: フレーム間コサイン類似度による繰り返し区間検出（①のメイン実装）。単一ファイル用`Detect`と、複数ファイルを跨いだ検出に対応する`ComputeHits`/`BuildCandidates`の分離版がある
- `Detection/BoundaryDetector.cs`: カット位置候補（音量・音色の急変点）の検出。詳細は「検出パイプラインの2段構成化」章
- `Detection/SegmentClassifier.cs`: カット位置で区切られた区間ごとのCM判定。同上
- `Detection/HistoryMatchDetector.cs`: 過去の確定履歴との一致度をフレーム単位で計算（`ComputeScores`）
- `Detection/LoudnessBoundaryRefiner.cs`: カット位置候補をRMSの局所最小点にスナップする補正（②の実装）。`RefineBoundaries`で境界リストを1点ずつ補正する
- `Pipeline/CmDetectionPipeline.cs`: 上記を束ねる。`DetectSingleAsync`（単一ファイル）と`DetectBatchAsync`（複数ファイル、ファイル間の繰り返しも検出）。CPU負荷の高い処理は`Task.Run`でバックグラウンドスレッドに退避済み

### 実装済み（App / WPF GUI）
- `MainWindow.xaml` / `MainWindow.xaml.cs`: 単一ファイル/フォルダ指定の切替、検出実行、ファイル一覧、波形＋CM候補ハイライト表示（Canvas上に自前描画）、候補ごとのカットON/OFF（DataGridのチェックボックス）、カット＋再結合／カットのみモードの切替、カット実行
- 波形のクリックでその位置から再生できる（`WaveformCanvas_MouseLeftButtonDown`→`StartPlayback(position, stopAt: null)`）。
  検出結果に関係なく自由に内容を確認するため、区間再生（前後1秒で自動停止）と違い停止操作があるまで流し続ける
- `FileResultItem.cs`: ファイル一覧表示用の軽量ラッパー

### ビルド・テスト確認済み
- `dotnet build RadioCmCutter.slnx` → 成功（0警告0エラー）
- `dotnet test src/RadioCmCutter.Core.Tests/...` → 47件全て成功（FFT・繰り返し検出・区間補集合計算・カット位置候補検出・境界補正・区間ごとのCM判定・複数ファイル一括処理でのファイル跨ぎ検出の単体テスト）。テストは実音声を使わず、合成した`FrameFeatures`・`DecodedAudio`を手組みする方式（`RepeatSegmentDetectorTests`の慣習に倣う）
- `dotnet run --project src/RadioCmCutter.App/...` → クラッシュせず起動・プロセス安定稼働を確認
- **未確認**: 実際の画面操作（ファイル選択・検出・波形表示・カット実行）そのものの動作。Claude Code側にネイティブWPFウィンドウを操作する手段がないため、ユーザー自身による実機確認が必要（起動自体・レイアウトはユーザーがスクリーンショットで確認済み。2026-09-11）
- **確認済み**: FFmpeg同梱・実行、およびCore側パイプライン（デコード→特徴抽出→繰り返し検出→境界補正→カット）のエンドツーエンド動作。合成音声（ノイズ番組＋同一CMトーン2回）で検証し、修正後は正しく検出できることを確認（下記「検出アルゴリズムの改善」参照）。検証はリポジトリ外の使い捨てコンソールプロジェクトで実施し、本体には残していない

### 検出アルゴリズムの改善（2026-09-11）

実データ相当の検証（ノイズ番組＋同一トーンCM2回、を合成音声で再現）で、**当初の実装には重大な誤検出バグがあった**ことが判明し、修正済み。

- **問題**: `FeatureExtractor`が「その瞬間のスペクトル形状」だけを特徴にしていたため、一定トーンや同じ統計的特徴を持つノイズ（ピンクノイズ等）のように**時間的に変化しない音**が、自分自身と誤って「繰り返し」判定されてしまい、本当のCM（トーンを2回挿入した箇所）の位置を検出できず、無関係な場所を候補として返していた
- **修正**: 各フレームの特徴ベクトルに、**直前フレームからの変化量（デルタ）を連結**するよう`FeatureExtractor.Extract`を変更（[FeatureExtractor.cs](src/RadioCmCutter.Core/Detection/FeatureExtractor.cs)）。静止的な音は「変化がない」という点でも自分自身と紛らわしくなりうるが、実際の番組（トーク・ノイズ等）はデルタ込みで見ると区別しやすくなり、検証で改善を確認済み
- **検証方法**: `tools/ffmpeg`同梱後、ffmpegで合成音声（ノイズ番組＋同一1000Hzトーンを2回挿入）を生成し、使い捨てのコンソールプロジェクト経由でCoreパイプラインを直接実行して確認（本体リポジトリには含めていない、一時的な検証手順）
- **検証結果**: 真のCM位置[30.0-45.0s][85.0-100.0s]に対し、検出結果[30.46-41.76s][1:25.02-1:37.02]と近い一致（境界の数秒のズレは音量境界補正が無音のないテスト音声では効きにくいため。実際の録音では前後に無音/フェードがあることが多く、より正確に働く見込み）

### 検出アルゴリズムの追加強化（2026-09-11 その2）

ユーザーから「検出が甘い（少ない）、音量・音色が変化した箇所（会話↔曲等）を積極的に拾ってほしい」との要望。
モデルなしでの精度向上として、繰り返し検出に依存しない**急変点ベースの検出**を追加。

- 新規: [TransitionSegmentDetector.cs](src/RadioCmCutter.Core/Detection/TransitionSegmentDetector.cs)。フレームごとの
  スペクトル変化量（`FrameFeatures.SpectralChangeMagnitude`、新規追加）とRMS（音量）変化を合成したスコアの
  上位パーセンタイル（既定85%）を「急変点」とし、急変点同士の間（既定4〜90秒）をCM候補として追加する
- `CmDetectionPipeline`で①繰り返し検出＋③急変点検出の両方を実行し、重複・隣接する候補をマージ（`MergeOverlappingCandidates`、確信度は最大値・繰り返し回数は合算）してから音量境界補正を行う
- 急変点ベースの候補は`RepeatCount=0`・確信度0.5前後で区別できるようにしている（繰り返し検出由来の候補は確信度が高くなりやすい）
- 検証: 合成音声（ノイズ番組＋同一CMトーン2回）で、繰り返し検出による2件の高確信度候補に加え、急変点由来の追加候補が複数出ることを確認（[検証ログ参照]。ノイズ内部の統計的揺らぎにも反応するため、実際のトーク録音での閾値調整が今後必要）

### カット時のビットレート引き継ぎ修正（2026-09-11）

- **問題**: `AudioCutter`のエンコード設定が常に固定`-b:a 192k`だったため、元ファイルがそれより低いビットレート
  （ラジオ録音のAAC/MP3は48〜96kbps程度が多い）の場合、カット後のファイルサイズが**3〜4倍に膨張**する不具合があった
  （ユーザー報告: 42MBのaac元ファイルに対し、カット中のファイルが170MB超になった）
- **修正**: [AudioDecoder.GetAudioBitrateBpsAsync](src/RadioCmCutter.Core/Ffmpeg/AudioDecoder.cs)でffprobeから元ファイルの
  ビットレートを取得し、[AudioCutter.BuildEncodingArgs](src/RadioCmCutter.Core/Ffmpeg/AudioCutter.cs)でそれを
  引き継ぐように変更（取得できない場合のみ128kbpsを既定値とする）
- 検証: 48kbps AAC(m4a)ファイルをカット（無編集の全区間コピー相当）した結果、出力も46.6kbps相当となり元とほぼ同サイズであることを確認

### WPF画面の操作性改善（2026-09-11）

ユーザーからのフィードバックに基づき対応:
- 入力欄の「参照...」「検出開始」ボタンの並び順を、操作の流れに合わせて `[パス入力]→[参照...]→[検出開始]` に変更（[MainWindow.xaml](src/RadioCmCutter.App/MainWindow.xaml)のDockPanelの子要素順序を変更。DockPanelは先に書いた要素ほど外側に配置されるため、順序の意味が直感と逆になりやすい点に注意）
- 波形表示の下に時間目盛り（Canvas「TimeRulerCanvas」、mm:ss表示、ファイル長に応じて目盛り間隔を自動選択）を追加
- 選択中のCM候補の開始/終了位置（前後3秒）を再生するボタンを追加（`System.Windows.Media.MediaPlayer`使用。Windows標準のMedia Foundationでmp3/aac/m4a/wavを再生でき、追加ライブラリ同梱は不要）。再生中は波形上に黄色い再生位置ライン（`_playheadLine`）を100ms間隔のタイマーで更新表示
- 検出時（ディレクトリ一括処理でのファイル読み込み進捗）とカット時（ファイルごとの進捗）に、それぞれ`ProgressBar`と「(i/n)」テキストを表示するように変更。`CmDetectionPipeline.DetectBatchAsync`に`IProgress<(int,int)>`パラメータを追加し、ファイル読み込みごとに進捗通知するようにした
- **修正したバグ**: カット完了後、画面下部（`FooterStatusText`）には完了メッセージが出るが、上部（`StatusText`）が「カット処理中...」のまま残る不具合があった（`SetBusy(false, StatusText.Text)`と自分自身の古い値を再代入していたのが原因）
- ユーザー方針により、その後**ステータス（進捗・完了）メッセージは`FooterStatusText`（画面最下部）に統一**し、`StatusText`（上部、入力欄の下）は**エラー・警告専用**に再整理した。`SetBusy`はボタンの有効/無効切替のみを担当するように変更し、テキスト設定は呼び出し側で明示的に行う方式にした。進捗バーも検出用・カット用の2つ（`DetectProgressBar`/`CutProgressBar`）を統合し、`FooterProgressBar`1つに一本化（最下部、`FooterStatusText`の上）
- 再生の前後秒数は当初3秒だったが、ユーザー要望で1秒に変更（`PlaybackPreviewSeconds`）

### 検出結果一覧を「途切れなく」表示するよう変更（2026-09-11 その3）

ユーザーから「CM候補だけを表示すると、CM候補の間（0:00-10:00, 13:00-18:00のように）が飛んで見え、
どこが『CMではないと判断された』のか分かりにくい」との指摘。ファイル全体を途切れなく分割し、
CM候補と非CM区間（検出外）を1つの一覧で表示するように変更。

- 新規: [TimelineSegmentRow.cs](src/RadioCmCutter.App/TimelineSegmentRow.cs)（App層のみ、Core非依存の表示用モデル）。
  `Build(totalDuration, candidates)`で、検出済みCM候補＋その間の区間（`AudioCutter.ComputeKeptSegments`の
  複数を流用）を結合し、開始時刻順に並べた「途切れないタイムライン」を作る
- `FileResultItem`に`TimelineRows`を追加（ファイル選択を切り替えても編集内容が保持されるよう保持）。
  DataGridの`ItemsSource`、カット実行時の対象判定（`item.TimelineRows.Where(r => r.CutEnabled)`）を
  `Result.Candidates`から`TimelineRows`に変更
- 「検出外」の行もチェックボックスで**手動でカット対象に追加できる**（検出漏れの救済）。逆にCM候補も
  チェックを外せば従来どおりカット対象から外せる
- 波形上のハイライトも、カット対象（赤・濃い）と検出はされたがカット対象外（黄色・薄い）で色分け

### CM検出理由の分類・音楽の長さ判定（2026-09-11 その4）

ユーザーから「音楽と会話の区別で判断できないか、CMは15/30/60秒等の尺で判断できるはず、番組内の音楽は
CMよりも長いことが多い」との提案。**音楽/会話の二値分類を検出のゲートにするのは、番組内の選曲（本編）を
誤ってカットしたり、音楽のない話芸のみのCMを見逃したりするリスクがある**ため採用しなかったが
（ユーザーとの相談で合意）、**尺（長さ）による分類**は`TransitionSegmentDetector`に統合した。

- `CmCandidate`に`DetectionReason`（`RepeatedContent`/`AcousticTransitionCmLength`/`AcousticTransitionLong`/`HistoryMatch`）を追加。画面には「検出理由」列として表示（[TimelineSegmentRow.cs](src/RadioCmCutter.App/TimelineSegmentRow.cs)の`Kind`）
- `TransitionSegmentDetector`で、急変点同士の間の長さが**CMスポット尺の基本単位（既定15秒）の倍数に近い**（既定許容±3秒）場合は`AcousticTransitionCmLength`（CM尺相当、デフォルトでカット対象ON）、そうでない長尺（既定300秒まで検出対象を拡大）は`AcousticTransitionLong`（番組内の音楽の可能性、デフォルトでカット対象OFF＝安全側）に分類する（`IsCloseToSpotLength`）
- `MergeOverlappingCandidates`で、繰り返し検出（`RepeatedContent`）が絡めば常に最優先の理由として扱う（最も信頼できる根拠のため）

### CM検出の過検出抑制（2026-09-11 その4・チューニング）

ユーザーから「会話の途中でも細かく切れすぎる」との指摘。`TransitionDetectionOptions`の既定値を調整:
- `ChangeScorePercentile`: 0.85 → **0.93**（上位7%のみを急変点とみなす。候補が大幅に減る）
- `MinSegmentSeconds`: 4.0 → **8.0**（繰り返し検出の`MinRunSeconds`と統一。短い相槌等を除外）
- `RmsJumpWeight`: 3.0 → **1.5**（会話は抑揚だけで音量が大きく揺れるため、音量変化の影響を下げ、音色（スペクトル）変化を相対的に重視）

### ユーザー確定履歴の学習機能（2026-09-11 その5）

ユーザー提案: 「行クリック→確認→CM判断→実行時に情報を残す」ことで、回を重ねるほど精度が上がる仕組みが欲しい。
番組ごとに分けず、**内容ベース（音響的な近さ）の判定なので他番組の同じCMにも効くため、グローバルに1つ蓄積**する方針で合意。
CMは「BGMは同じでトーク部分だけ違う」ことが多いため、**CM全体ではなくフレーム単位（0.5秒）で指紋を保存し、部分一致でも検出できる**設計にした。

- 新規: [History/CmHistoryEntry.cs](src/RadioCmCutter.Core/History/CmHistoryEntry.cs) — 1件の確定区間（CM or 非CM）のフレーム特徴ベクトル列
- 新規: [History/CmHistoryStore.cs](src/RadioCmCutter.Core/History/CmHistoryStore.cs) — JSON永続化（`System.Text.Json`、追加ライブラリ不要）。保存先は`%LocalAppData%\RadioCmCutter\cm_history.json`（`GetDefaultFilePath()`）。壊れたJSONは無視して空から再開（検出処理は止めない）
- 新規: [Detection/HistoryMatchDetector.cs](src/RadioCmCutter.Core/Detection/HistoryMatchDetector.cs) — 対象ファイルの各フレームと、履歴の「確定CM」フレーム群とのコサイン類似度（最大値）を計算し、閾値（既定0.93）以上が連続する区間を候補化。「確定非CM」フレームとの類似度も計算し、非CM側の方が近ければ候補にしない（繰り返しがちな誤検出パターンの抑制）。`Reason=HistoryMatch`
- `CmDetectionPipeline`に`CmHistoryStore?`を追加（省略可・後方互換）。①繰り返し検出②急変点検出③履歴照合の3つを統合
- App側: `MainWindow`起動時に履歴をロードしパイプラインに渡す。**カット実行時**に、その時点の`TimelineRows`の判断（`CutEnabled=true`→確定CM、検出されたのに`CutEnabled=false`→確定非CM、未判断の検出外区間は保存しない）を履歴に追加・保存（`SaveHistoryFromUserDecisionsAsync`、ベストエフォート・失敗してもカット自体は継続）
- 検証: 使い捨てコンソールプロジェクトで「1回目検出→確定分を履歴保存→JSON再読込→履歴のみで同一ファイルを再検出」の一連の流れが正しく動作することを確認（保存→リロード→一致検出まで一貫して機能）
- **既知の制約**: `HistoryMatchDetector`は対象フレーム×履歴フレーム全件の総当たり比較（O(フレーム数×履歴フレーム数)）。履歴が大量に蓄積すると検出が遅くなる可能性があり、将来的に近似近傍探索等の高速化が必要になるかもしれない
- 併せて判明した既存バグ: `CmDetectionPipeline.DetectBatchAsync`内で`FeatureExtractor.Extract`（CPU負荷の高い同期処理）がバックグラウンドスレッドに退避されておらず、ディレクトリ一括処理時にUIスレッドをブロックしていた。`Task.Run`でラップして修正

### 検出パイプラインの2段構成化（2026-09-11 その6・ブランチ`feature/detection-tuning-2`）

実データ（`FUTURESCAPE_202502080900.aac`・2時間）でユーザーがCM位置を実地確認した結果、
**音響急変＋長さ一致（15秒の倍数近似）だけを根拠にしたCM判定は誤検出が多すぎる**ことが判明
（CMが1件も無いと確認済みの約10分間に「CM尺」判定が6件）。加えて、3つの検出器がそれぞれ
独自に区間境界を計算し、後から重なりを解消する構造そのものが誤判定の温床になっていた
（結合・境界補正のあとで分類を再評価せず、306秒の本編が「CM尺」扱いになるバグが発生）。

ユーザー方針により、**①「どこで切るか」（カット位置候補）→②「それがCMか」（区間ごとの判定）**
の2段構成に再構成した。精度はユーザー確定履歴の蓄積で補う前提。

- **①カット位置候補**: [BoundaryDetector.cs](src/RadioCmCutter.Core/Detection/BoundaryDetector.cs)
  （旧`TransitionSegmentDetector`を改名・再構成）。変化スコア（スペクトル変化＋音量の跳ね）の
  上位パーセンタイル（既定**0.88**）を切れ目とする。CM判定を兼ねないので再現率重視に振っている
  （余分な境界は後段で「CMではない」と分類されるだけ）。`MinBoundaryGapSeconds`（既定2秒）以内に
  密集した境界は**最も変化が強い1点**に畳む
- **境界の精緻化**: [LoudnessBoundaryRefiner.RefineBoundaries](src/RadioCmCutter.Core/Detection/LoudnessBoundaryRefiner.cs)。
  候補ごとにStart/Endを個別補正するのをやめ、**共有された境界リストを1点につき1回だけ**補正する。
  探索範囲を隣の境界との間にクランプして追い越しを防ぎ、範囲が潰れる場合は補正しない。
  ファイル先頭・末尾は補正対象外（スナップすると実音声を削る／余らせる）
- **②区間ごとのCM判定**: [SegmentClassifier.cs](src/RadioCmCutter.Core/Detection/SegmentClassifier.cs)。
  根拠の強い順（繰り返し出現→確定履歴一致→音響急変のみ）に判定し、**裏付けのある根拠**
  （`RepeatedContent`/`HistoryMatch`）だけをデフォルトのカット対象ONにする。音響急変のみの区間は
  CM尺/長尺のラベルを付けて表示するが**常にカットOFF**（ユーザーが手動でON）
- **重要な設計判断**: 判定は「区間内の一致フレームの割合」ではなく**「最小run長を満たす連続一致が
  区間内に存在するか」**で行う。割合で見ると、40秒の区間に10秒の本物の繰り返しが含まれる場合に
  0.25となり、各検出器が本来持っている最小run長の担保（繰り返し8秒・履歴3秒）を無関係な周辺音声の
  長さで薄めてしまうため（設計レビューでの指摘）
- **候補として出す範囲**: 裏付けのある根拠は長さに関わらず出す。音響急変のみの区間は
  8〜300秒のものだけ（範囲外は候補にせず、App側の「検出外」補完に任せる）。全区間を候補化すると
  `TimelineSegmentRow.IsDetectedCandidate`がほぼ全域でtrueになり、波形が塗り潰されるうえ、
  **ユーザーが確認していない本編まで`cm_history.json`に「確定非CM」として保存されて履歴が汚染される**
  （履歴で精度を補うという目的自体に反する）。この判断によりApp層は一切変更不要になっている
- **弱い根拠の区間は結合しない**（実装時に一度結合してしまい、確認用の行が全て消える不具合を出した）。
  隣り合う弱い区間の間にあるのは実際に検出された切れ目であり、結合すると「どこで切れるか」という
  情報を捨ててしまう。結合するのは裏付けのある根拠が同じ隣接区間のみ（1つのCMが余分な境界で
  分断された場合に1件にまとめるため）

テストで見つけて修正した実バグ2件（`LoudnessBoundaryRefiner`）:
- 隣接する境界が同じ無音に吸い寄せられて**同一位置にスナップし、長さ0の区間**ができていた
- 同程度の音量の候補点が複数ある場合（定常的な音楽・長い無音）に**最も早い点**を選んでいたため、
  境界が検出位置から最大3秒も引き離されていた。音量は微調整の手がかりに過ぎず切れ目の位置自体が
  主たる根拠なので、同程度に静かな点の中では**検出位置に最も近い点**を選ぶよう修正

実データでの検証結果（ユーザー報告のグラウンドトゥルースとの突き合わせ）:

| percentile | 候補数 | カットON | CMなし区間の誤カットON | 2件目の繰り返し検出の範囲 |
|---|---|---|---|---|
| **0.88（採用）** | 264 | 2件/47秒 | 0件（従来6件） | 01:54:38-01:55:11（33秒） |
| 0.93 | 263 | 2件/61秒 | 0件 | 01:54:38-01:55:24（46秒） |
| 0.97 | 180 | 2件/343秒 | 0件 | 01:50:46-01:56:14（**328秒**） |

0.97では境界が粗すぎて、繰り返し検出の根拠が巨大な区間に張り付き5分半がカットON扱いになる。
**境界を細かく取ることが実際のカット範囲の精度に効く**ことが実データで確認できた。
真陽性（00:20:45-00:21:00・確信度97%）は全設定で維持、306秒の本編もカットOFFのまま。

**現状の性質（重要）**: デフォルトでカットONになるのは繰り返し・履歴で裏付けられた区間のみなので、
単一ファイル・履歴なしの状態では検出量が少ない（2時間で47秒）。
これは意図した挙動で、①ディレクトリ一括処理で複数日分を渡してファイル間の繰り返しを効かせる、
②ユーザーが確認した結果を履歴に蓄積する、の2つで精度を上げていく設計。

### カット位置検出を「前後の窓の比較」に変更（2026-09-12）

ユーザー報告: 「切れ目のない同一BGMの中でも細かくカットされてしまう」「1:20-6:18は1曲なのに
4:55.87で分割されている」。実データで原因を調査し、2つのバグを特定・修正した。

**原因①**: 変化スコアが**隣接フレーム間（0.5秒）の瞬間的な変化量**だったため、連続した同一BGMの
中でも「話し始め・話し終わり」「曲中の楽器・ボーカルの入り」といった局所的な音響イベントに
反応していた（4:55.87の分割は、閾値をわずか19%超えた弱い境界。境界補正はほぼ無関係で
295.94→295.88秒しか動いていない）。

→ **境界の前後それぞれ8秒を平均して比較する方式**に変更（`BoundaryDetector.ComputeNoveltyScores`、
`NoveltyWindowSeconds`で調整可能）。「前後で持続的に音の性質が変わったか」を見るため、
局所的な出来事のスコアは桁違いに小さくなる。スペクトル形状の比較には
`FrameFeatures.SpectralVector`（新規・そのフレーム単体のスペクトル形状）を使う
（`Vector`に含まれるデルタ成分は窓内で平均するとほぼ打ち消し合うため）。
RMS（音量）項は精度を悪化させたため削除した（トークの抑揚に反応するため）。
これに伴い`FrameFeatures.Rms`・`SpectralChangeMagnitude`は未使用となり削除。

実データでの効果（ユーザー確認済みの正解3点＝12.82秒のジングル終わり・80.14秒のトーク→曲・
378.26秒の曲→次パート、で評価）:

| | 0:13-1:20の密度 | 6:18-16:10の密度 | 正解3点の位置精度 | 曲の途中(295.9秒) |
|---|---|---|---|---|
| 瞬間変化（修正前） | 9.0件/分 | 10.1件/分 | 0.2/1.6/0.3秒差 | 誤検出する |
| **前後8秒窓（採用）** | **3.6件/分** | **2.1件/分** | **0.3/1.1/0.2秒差** | 26秒離れて検出なし |

過検出が3〜5分の1になり、かつ切れ目の位置精度も向上している（窓16秒まで広げると
80.14秒の切れ目を見逃すため、8秒が上限）。

**原因②（①の修正で発覚）**: 繰り返し検出の真陽性が1件消えた。`RepeatSegmentDetector.ComputeHits`は
**既に最小run長（既定8秒）のふるいを適用済み**（`hitCount>0`のフレームは全て8秒以上のrunの一部）
なのに、`SegmentClassifier`が区間ごとに再度8秒の連続一致を要求していたため、**条件の二重適用**に
なっていた。実データの繰り返しrunは2件とも9.2秒しかなく、カット位置候補がrunの中に落ちて
8.6秒+0.6秒に分断されると丸ごと脱落していた。

→ 区間側の要求を`MinRepeatEvidenceRunSeconds`（既定2秒）に緩和し、上流のふるいを信頼する形にした
（散発的な単発ヒットで区間全体を繰り返し扱いにしないための下限としては維持）。
これで真陽性2件が復活（00:20:45-00:20:56、01:54:34-01:54:47、いずれも確信度97%）。

**残っている課題**: 1:20-6:18の1曲は、まだ4分割される（01:51/02:02/02:12/05:30）。
曲内の構成変化（イントロ→ボーカル→間奏等）は前後8秒の窓でも実際に音の性質が変わるため、
「曲内の構成変化」と「項目間の切れ目」の区別は音楽構造解析なしでは原理的に難しい。
候補総数は264件→109件に減っている。次の手として「隣接する弱い区間どうしの平均スペクトルが
似ていれば統合する」案があるが、繰り返し・履歴の裏付けがない本物のCMが隣の本編と統合されて
確認しづらくなる副作用があるため、ユーザー判断待ち（2026-09-12時点）。

### 論文に基づくカット位置検出の改良（2026-09-12 その2）

ユーザー提案「テレビ・ラジオ番組のシーン検出の論文を参照してアルゴリズムを考案したほうが
現実的では」に基づき文献調査を実施。その結果、**自前の実装が既知手法の劣化版だった**ことが判明し、
正式な形に修正した。

**先に否定された案**: 「隣接する弱い区間の平均スペクトルが似ていれば統合する」案（ユーザー承認済みだったが、
実データで測定した結果**成立しないことが判明したため実装しなかった**）。
ユーザー確認済みの真の切れ目の類似度は 0.9944/0.9952/0.9643、統合したい曲の途中は
0.9778〜0.9994 で**完全に重なっており分離不能**。区間全体の平均スペクトルは
「その局の音作り・同じBGM・同じ話者」に支配され、ジングルかトークか曲かをほとんど区別しない。
識別情報は境界での局所的な変化にあり、全体平均にはない。

**改良①: Footeのチェッカーボードカーネル法**
（J. Foote, "Automatic Audio Segmentation Using a Measure of Audio Novelty", ICME 2000）。
正式な新規性スコアは2項からなる:
- **凝集項**: 前半どうし・後半どうしの相似（それぞれの側がまとまっているか）
- **交差項**: 前半と後半の相似（両側が同じ内容か）

自前実装は**交差項だけ**で凝集項が抜けていた。凝集項は「トークに曲がかぶっている」ような
*中身は雑多だが続いている*区間を抑制する働きがある（両側とも「まとまりが無い」と評価されるため
スコアが下がる）。カーネル中心から離れるほど重みを下げるガウス窓も併せて導入。

**改良②: Dixonの適応的ピーク選出**（S. Dixon, "Onset Detection Revisited", DAFx-06）。
ファイル全体一律のパーセンタイルでは、静かなトークと大音量の曲を同じ絶対尺度で判定するため、
長時間ファイルでは必ず片方が過検出になる。「近傍で最大」かつ「局所平均＋`PeakThresholdInStdDev`×
全体のばらつき を超える」という局所的な基準に変更した。

実データでの効果（ユーザー確認済みの正解3点で評価）:

| しきい値 | 正解3点の位置精度 | 0:13-1:20 | 6:18-16:10 | 曲内の誤分割 | カットON |
|---|---|---|---|---|---|
| 0.3σ | 全て0.0秒差 | 2.7件/分 | 3.3件/分 | 7件 | 2件/23秒 |
| 0.5σ | 全て0.0秒差 | 0.9件/分 | 1.9件/分 | 4件 | 2件/27秒 |
| **0.8σ（採用）** | **全て0.0秒差** | **0件** | **0.7件/分** | **3件** | 2件/27秒 |
| 1.2σ | 全て0.0秒差 | 0件 | 0.2件/分 | 2件 | 2件/**407秒** |

正解3点が**すべて誤差0.0秒**で検出される（改良前は0.3/1.1/0.2秒差）。
1.2σまで上げると境界が粗くなりすぎ、繰り返し検出の根拠が巨大な区間に張り付いて
カット範囲が3分規模に膨張するため、0.8σを上限とした。

**今後の選択肢（文献より、未実装）**:
1. **ランドマーク指紋＋時間差ヒストグラム**（A. Wang, ISMIR 2003 / Ogle & Ellis, ICASSP 2007）:
   現在のO(フレーム数^2)の繰り返し検出を定数時間のハッシュ検索に置き換えられる
   （6時間の資料に対し5秒クエリが約130ms）。複数日分の一括処理を実用化する鍵。
   C#で外部ライブラリなしに実装可能。ただしジングル・音楽ベッドには強いが、
   単一トーンのビープ音や生読みの音声のみのCMには弱いと報告されている
2. **Covell, Baluja & Fink (IEEE MMSP 2006, Google)**: 音声のみの繰り返しでCM検出（精度87%/再現率94%）。
   構造ルール「復元区間が120秒超は番組の再放送として棄却、8秒未満は偶然として棄却」は今すぐ流用可能。
   「確信できる一致から外側へ境界を広げる」設計（＝現在の「境界→分類」と逆順）を推奨している。
   なお「4日間で12.5%のCMは1回しか流れない」という限界も報告されており、繰り返しだけでは原理的に拾えない
3. **Duygulu et al. (ICME 2004)**: CMはブロックで並ぶため「確定CM同士の短い隙間もCMとみなす」後処理
4. BIC（Chen & Gopalakrishnan 1998 / Cettolo et al. 2005）は原理的には有力だが、
   パーセンタイルがλという別のつまみに変わるだけで、かつ低次元MFCC前提・話者変化での検証のため見送り

### 評価ハーネスの追加（2026-09-12 その3）

これまで「実データで動かして目視で良さそう」を繰り返しており、アルゴリズム変更が効いたのか
たまたまそのファイルに合っただけなのかを区別できていなかった。特にこの問題では
**「3秒ごとに機械的に区切る」だけでもF値0.5前後が出る**ため、ベースラインと比較しない限り
数値の良し悪しを判断できない。そのための採点の仕組みを追加した。

- `Evaluation/BoundaryEvaluation.cs`: 区切りの適合率・再現率・F値（許容誤差0.5秒と3秒）と中央値ずれ。
  1つの正解の周りに推定を並べれば当たる、という抜け道を塞ぐため**1対1で対応付ける**。
  ファイル先頭・末尾は自明なので採点から除外する（`Trim`）
- `Evaluation/BoundaryGroundTruth.cs`: 正解の区切り時刻のJSON保存・読込。
  保存先は**音声ファイルと同じ場所**（`<音声ファイル>.boundaries.json`）。
  ユーザーが中身を確認・手直しできるようにするため、隠れた場所には置かない
- `Evaluation/BoundaryBenchmark.cs`: 検出結果と3つのベースライン（正解と同じ密度で等間隔／3秒ごと／
  検出結果と同数のランダム）を同じ指標で並べて採点する
- App: 「正解として保存」ボタン（画面で修正した区切りを正解として書き出す）と
  「精度を測定」ボタン（保存済みの正解と**手動修正前の検出結果**を突き合わせ、結果をクリップボードにコピー）

**確定履歴（`cm_history.json`）との役割の違いに注意**（ユーザーからも質問があった点）:

| | 確定履歴 | 評価用の正解データ |
|---|---|---|
| 役割 | 検出の**材料**（入力） | 検出結果の**採点基準** |
| 中身 | 確定区間の音響指紋（ベクトル列） | 区切りの時刻リストだけ |
| 検出時に使うか | 使う | **絶対に使わない** |
| 位置づけ | 製品の機能 | 開発用の道具 |

正解データを検出に渡すと、答えを見ながら答案を書くことになり精度が測れなくなる。
両方ともユーザーの確認作業から生まれるが、片方はアルゴリズムに渡し、
もう片方は意図的に隠しておく必要がある。
また評価ハーネスは**製品の動作を変えない**。測るのはむしろ「初回利用（履歴ゼロ）の性能」。

#### 実データで判明した落とし穴: 部分的な注釈のまま全体を採点してはいけない

初回の測定で以下の結果が出た（正解3件・検出116件、2時間のファイル全体を採点）:

```
検出結果  F=0.051 (適合率=0.026 再現率=1.000) 一致=3/3件 中央値ずれ 正解→推定=0.01秒
```

**再現率1.000・ずれ0.01秒は本物の成果**（確認済み3点をすべて0.5秒以内で検出）。
一方**適合率0.026は無意味**で、正解が3件しかないのに検出116件の残り113件を機械的に
「外れ」と数えているだけ。2時間の番組に区切りが3つしかないはずはなく、注釈が未完成なだけである。

このため`BoundaryGroundTruth`に`AnnotatedRangeStartSeconds`/`AnnotatedRangeEndSeconds`を設け、
**確認し終えた範囲だけを採点する**ようにした。ベースラインも同じ範囲で作る。
画面から保存するとファイル全体が確認済みとして記録されるので、
一部しか確認していない場合は正解ファイル（JSON）のこの値を手で書き換える。
この落とし穴は`Run_PartiallyAnnotatedFile_ScoresOnlyTheAnnotatedRange`でテストとして固定してある。

**次に測定するときは、まず連続した一区間（例: 冒頭20分）を最後まで確認し、
その範囲を`AnnotatedRange…`に指定すること。** そうしないと適合率が読めない。

### 既知の課題・今後の改善余地
- `RepeatSegmentDetector`は素朴な総当たり（O(フレーム数^2)）実装。長時間音声・多数ファイル一括処理では時間がかかる可能性があり、将来的に高速化（ブロック化、間引き、ハッシュベースの事前絞り込み等）の余地がある
- 類似度閾値・最小run長・最小間隔などのパラメータ（`RepeatDetectionOptions`）、カット位置候補のパーセンタイル・境界集約間隔（`BoundaryDetectionOptions`）、候補として出す長さの範囲（`SegmentClassificationOptions`）は引き続きチューニングの余地がある
- デルタ特徴を追加してもなお、**完全に無音・完全に一定のトーンが長時間続く区間**は、理論上「自分自身との繰り返し」と区別できない（変化が全くない音は、どの瞬間を切り取っても同じに見えるため）。実際のトーク番組ではまず起きないが、長い無音・一定音のテスト信号等では注意
- 急変点検出はノイズの統計的揺らぎにも反応しやすく、誤検出（過検出）が増える可能性がある。確信度が低め（0.5前後）に出るようにしてはあるが、実データでの閾値調整が必要
- FFmpegは`src/RadioCmCutter.App/tools/ffmpeg/`に同梱済み（BtbN/FFmpeg-Builds LGPL sharedビルドより取得。別プロセス起動のみでライブラリはリンクしていない）

## FFmpeg同梱について（対応済み・リポジトリに同梱）

配布時に別PCでの追加インストールを避けるため、`src/RadioCmCutter.App/tools/ffmpeg/`に配置済み（csprojで出力ディレクトリへ自動コピーされる設定済み、[RadioCmCutter.App.csproj](src/RadioCmCutter.App/RadioCmCutter.App.csproj)参照）。**このフォルダはリポジトリにコミットされている**（`.gitignore`からは除外済み）。

- 入手元: **BtbN/FFmpeg-Builds**（GitHub Releases）。当初gyan.devを使う予定だったが、gyan.devのサーバーが極端に遅かった（実測40KB/秒程度、GitHubは5MB/秒程度）ため切り替えた
- ビルド種別: `ffmpeg-master-latest-win64-lgpl-shared.zip`（LGPL、shared=DLL分割構成）を使用。当初はGPLの静的ビルド（`ffmpeg-master-latest-win64-gpl.zip`）だったが、**ffmpeg.exe/ffprobe.exeが157MB前後あり、GitHubの1ファイル100MB上限を超えて通常pushできなかった**ため切り替えた。sharedビルドはffmpeg.exe/ffprobe.exeが数百KBと小さく、実体は各DLL（avcodec等、最大でも90MB程度）に分散するため、Git LFSなしでpushできる
- 同梱ファイル: `ffmpeg.exe`, `ffprobe.exe`, `avcodec-63.dll`, `avdevice-63.dll`, `avfilter-12.dll`, `avformat-63.dll`, `avutil-61.dll`, `swresample-7.dll`, `swscale-10.dll`, `LICENSE.txt`（`ffplay.exe`は本アプリで不要なため同梱していない）
- ライセンス: LGPL（動画コーデックのGPL部分を除いたビルド）。mp3/aac/m4a/wavの処理には影響なし（libmp3lame含めLGPL範囲で提供されている）。本アプリはffmpeg.exeを別プロセスとして呼び出すだけでライブラリをリンクしていない。同梱した`LICENSE.txt`は削除しないこと
- DLLのバージョン番号（-63, -12等）はビルドが更新されると変わる可能性があるため、将来ffmpegを更新する際はbinフォルダの全ファイルを一致するセットで入れ替えること（exeと不整合なバージョンのDLLが混在するとロードエラーになる）

## Git / GitHub

- リモート: https://github.com/shuokitsu/RadioCmCutter （個人アカウント。**現在Public**、ユーザー了承済み・当面このまま）
- ローカルgit設定はこのリポジトリのみの`--local`設定（`user.name=shuokitsu`, `user.email=shuokitsu@noreply.local`。実在メールではない仮設定）
- 認証はPAT（`shuokitsu`アカウントのPersonal Access Token, classic, repoスコープ）を使用。ブラウザOAuth（Git Credential Manager）は法人アカウントとの混在で使えなかったため回避した
- push時は `git -c credential.helper= push` のように資格情報ヘルパーを一時的に無効化し、ユーザー自身のターミナルでユーザー名/PATを直接入力する運用（トークンをClaude Codeに渡さないため）
- コミット済み（雛形一式＋FFmpeg同梱＋検出改善）。`tools/ffmpeg/`は当初`.gitignore`で除外していたが、ユーザー要望により**リポジトリに同梱する方針に変更**（詳細は「FFmpeg同梱について」章）

## 次のアクション

1. **連続した一区間を最後まで注釈する**（例: 冒頭20分）。画面で区切りを確認・修正し「正解として保存」。
   一部しか確認していない場合は、保存された `<音声ファイル>.boundaries.json` の
   `AnnotatedRangeStartSeconds` / `AnnotatedRangeEndSeconds` を確認済みの範囲に書き換える。
   これをやらないと適合率が読めない（上記「落とし穴」参照）
2. その状態で「精度を測定」し、**現行アルゴリズムの基準値を記録する**。以降の変更はすべてこれと比較する
3. [SEGMENTATION_SPEC.md](SEGMENTATION_SPEC.md) の実装に着手。着手順は仕様書の「実装順序」に従う:
   - [A] 解析用デコードを 8kHz → **16kHz** に変更（以降すべての前提。ロールオフ・ZCR・スペクトル変化量は
     4kHz以上の成分に依存しており、現在はその識別力を捨てている）
   - [B] **時間ラグ行列による繰り返し拒否**（最重要。「1曲の途中で切れる」への唯一の解）
   - [C] 音楽/トークのラベル付けと同ラベル統合（トーク＋BGMの過剰分割対策）
4. ディレクトリ一括処理での検証（同番組の複数日分。ファイル間の繰り返しが効くかどうか）
5. ユーザーによるWPF画面の実機動作確認の継続

### 積み残し・未確認事項

- `FUTURESCAPE_202502080900.aac` の16:10以降のCM位置はユーザー未確認
- 2件のRepeatedContent検出（00:20:45-00:20:56、01:54:34-01:54:47、確信度97%）が
  本当にCMかどうかはユーザー未確認。真陽性として扱ってきたが裏は取れていない
- 1:20-6:18の1曲内に3件の分割が残っている（改良前は4件）
- `RepeatSegmentDetector`・`HistoryMatchDetector`はO(n^2)の総当たり。
  複数日分の一括処理を実用化するにはランダムアクセス可能な指紋（Wang/Ogle & Ellis）への
  置き換えが必要（[SEGMENTATION_SPEC.md](SEGMENTATION_SPEC.md) 参照）
