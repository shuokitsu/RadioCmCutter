# プロジェクト: ラジオCM自動カットアプリ

## 重要な前提

- **本プロジェクトディレクトリは検討完了後に削除される予定**（ユーザー言及: 2026-09-11）
- 本ファイルは別PC等での作業引き継ぎのため、決定事項・保留事項を含む情報を**すべて**記録する目的で作成している
- 詳細仕様は [SPEC.md](SPEC.md) にも記載（本ファイルと内容重複あり。本ファイルが最新の決定事項を優先）
- 2026-09-11時点: **仕様検討フェーズ。実装は未着手**

## プロジェクトの目的

ラジオ番組の録音ファイルからCM区間を自動検出してカットするWindows向けローカルアプリを作る。

### 元の要望（ユーザー原文ベース）

- zip解凍してexe実行で動くのが理想
- ライブラリ（DLLなど）の利用はOKとするが出来れば同梱したい（別PCで利用時に別途FFmpegインストールなどは出来れば避けたい）
- 外部APIなどネット経由のものは一切使わない
- ごく軽量のローカルLLMであればOKとするが、将来的にAndroidアプリ化も検討しているので使わないほうがいいかもしれない
- CM部分の検出アルゴリズムについてはアイデアなし、提案を求める
- 対応ファイル形式はmp3, aac, m4aは必須、他対応容易であれば追加（wavなど）
- カットして自動再結合するモードと、カットのみモード（動作確認も兼ねる）を実装
- 画面上で検出結果の確認ができるとなお良い（スキップせずそのままカットも可）（難がある場合は見送ってもよい）
- 1ファイル指定で実行と、ディレクトリ指定で連続実行が可能

## 決定事項

| 項目 | 決定内容 |
|---|---|
| 配布形態 | zip解凍→exe実行。インストーラー不要 |
| 外部通信 | 一切なし（API通信、アップデートチェック等も含めない） |
| 対応ファイル形式 | 必須: mp3, aac, m4a／追加候補: wav等（FFmpeg同梱前提なので追加コストは低い） |
| 動作モード | ①カット＋自動再結合モード ②カットのみモード（動作確認用） |
| 実行方式 | 単一ファイル指定 ／ ディレクトリ指定バッチ処理（両方実装） |
| CM検出アルゴリズム | **①繰り返しセグメント検出（メイン）＋②音量・ラウドネス境界検出（補助・境界精緻化用）** のハイブリッド方式。詳細は本ファイル「CM検出アルゴリズム」章、および[SPEC.md](SPEC.md) |
| 確認UI | 波形表示＋検出区間のハイライト表示。区間ごとにカット対象ON/OFF切替可能にしてから最終カット実行 |
| ローカルLLM/STT | **今回は導入しない**。将来、文字起こし等の追加機能も含めて再検討予定（下記「今後の検討事項」参照） |
| 実装言語・フレームワーク | **確定: C#/.NET 10 (LTS), WPF**。2026-09-11時点で.NET8はサポート終了間近（2026年11月）、.NET9はSTSで既にサポート終了のため、最新LTSの.NET10を採用。将来Android化する場合は .NET MAUI でコード資産を再利用しやすい点も理由の一つ |

## CM検出アルゴリズム（詳細）

### ①繰り返しセグメント検出（メイン）
- 同一CM音源は同一番組内や別日の録音間で繰り返し使用される傾向があるという前提
- 音声を一定間隔（0.5〜1秒程度）のチャンクに分割し、音響特徴量（MFCC等）を抽出
- チャンク間（ファイル内・ファイル間）の類似度を計算し、高い類似度で繰り返し出現する区間をCM候補としてマーキング
- ディレクトリ一括処理（同番組の複数日分）と相性が良く、処理対象が増えるほど精度が向上する

### ②音量・ラウドネス境界検出（補助）
- CM区間はコンプレッサーがかかり音量が均一で大きい傾向がある、という特徴を利用
- ①で検出した候補区間の開始・終了位置を、RMS/ラウドネスの変化点や無音区間を基準に精緻化する
- 単独でのCM/本編判別には使わず、境界のズレ補正用途に限定

### 見送り・将来検討
- ジングル・シグナル音のテンプレートマッチング（番組ごとの手動登録が必要なため今回は見送り）
- ローカルSTT/LLMによるキーワード検出（下記参照）

## 今後の検討事項（保留中）

1. **ローカルLLM/STTの将来的な導入**
   - 今回は不使用の方針で確定
   - 将来、文字起こし機能等の追加価値も含めて再検討予定（ユーザー: 2026-09-11「導入の際は文字起こしなど追加機能も出来そうなので、後日検討」）
   - 検討時の論点: CM特有フレーズ（「お問い合わせは」等）検出による精度向上 vs モデル同梱によるサイズ増加・Android移植時のSTT実行コスト

## 未着手の細部（実装フェーズで詰める）

- CM候補区間の最小長・最大長の閾値、繰り返し判定の類似度閾値などのパラメータ
- 誤検出時の学習・調整機能の有無（ユーザーによる手動補正結果を次回以降に活かすか）
- 出力ファイルの命名規則、出力先ディレクトリ構成
- 処理ログ・レポート（検出区間の時刻リストをテキスト等で出力するか）

## 開発環境

- **.NET 10 SDK**（Windows x64, SDK版）をインストール（[dotnet.microsoft.com/ja-jp/download](https://dotnet.microsoft.com/ja-jp/download)）。Runtimeのみではなく必ずSDKを選ぶ
- WPF利用のため追加ワークロードのインストールは不要（.NET SDKに標準で含まれる）
- エディタ: VSCode + 拡張機能「C# Dev Kit」（Microsoft公式）

## 実装状況（2026-09-11時点）

### プロジェクト構成
- `RadioCmCutter.slnx`（ソリューション、.slnx形式）
- `src/RadioCmCutter.Core`: 音声解析・検出・カットのロジック（UI非依存、将来のAndroid化時に再利用する想定）
- `src/RadioCmCutter.App`: WPFアプリ本体（net10.0-windows）
- `src/RadioCmCutter.Core.Tests`: xUnitテスト（Coreロジックのみ対象。`dotnet test`で実行可能）

### 実装済み（Core）
- `Ffmpeg/FfmpegLocator.cs`: ffmpeg.exe/ffprobe.exeの実体を`<実行ファイルの場所>/tools/ffmpeg/`から探す。無ければPATH上のものにフォールバック
- `Ffmpeg/AudioDecoder.cs`: ffmpegでモノラル8kHz PCMにデコード（解析用）、ffprobeで長さ取得
- `Ffmpeg/AudioCutter.cs`: 削除区間の補集合（残す区間）を計算し、ffmpegで①結合カット②個別書き出し（カットのみモード）を行う
- `Detection/Fft.cs`: 自作Radix-2 FFT（外部ライブラリ依存を避けるため自前実装）
- `Detection/FeatureExtractor.cs`: 0.5秒フレームごとに対数帯域エネルギー特徴（24バンド、L2正規化）を抽出
- `Detection/RepeatSegmentDetector.cs`: フレーム間コサイン類似度による繰り返し区間検出（①のメイン実装）。単一ファイル用`Detect`と、複数ファイルを跨いだ検出に対応する`ComputeHits`/`BuildCandidates`の分離版がある
- `Detection/LoudnessBoundaryRefiner.cs`: RMSの局所最小点に境界をスナップする補正（②の実装）
- `Pipeline/CmDetectionPipeline.cs`: 上記を束ねる。`DetectSingleAsync`（単一ファイル）と`DetectBatchAsync`（複数ファイル、ファイル間の繰り返しも検出）。CPU負荷の高い処理は`Task.Run`でバックグラウンドスレッドに退避済み

### 実装済み（App / WPF GUI）
- `MainWindow.xaml` / `MainWindow.xaml.cs`: 単一ファイル/フォルダ指定の切替、検出実行、ファイル一覧、波形＋CM候補ハイライト表示（Canvas上に自前描画）、候補ごとのカットON/OFF（DataGridのチェックボックス）、カット＋再結合／カットのみモードの切替、カット実行
- `FileResultItem.cs`: ファイル一覧表示用の軽量ラッパー

### ビルド・テスト確認済み
- `dotnet build RadioCmCutter.slnx` → 成功（0警告0エラー）
- `dotnet test src/RadioCmCutter.Core.Tests/...` → 10件全て成功（FFT・繰り返し検出・区間補集合計算の単体テスト）
- `dotnet run --project src/RadioCmCutter.App/...` → クラッシュせず起動・プロセス安定稼働を確認
- **未確認**: 実際の画面操作（ファイル選択・検出・波形表示・カット実行）そのものの動作。Claude Code側にネイティブWPFウィンドウを操作する手段がないため、ユーザー自身による実機確認が必要
- **未確認**: FFmpeg実行（バイナリ未配置のため。下記参照）

### 既知の課題・今後の改善余地
- `RepeatSegmentDetector`は素朴な総当たり（O(フレーム数^2)）実装。長時間音声・多数ファイル一括処理では時間がかかる可能性があり、将来的に高速化（ブロック化、間引き、ハッシュベースの事前絞り込み等）の余地がある
- 類似度閾値・最小run長・最小間隔などのパラメータ（`RepeatDetectionOptions`）は初期値の当て推量。実データでのチューニングが必要
- FFmpeg未同梱のため、`FfmpegLocator.TryVerify()`は現時点では失敗し、起動時に警告が出る（クラッシュはしない）

## FFmpeg同梱について（要ユーザー判断・未着手）

配布時に別PCでの追加インストールを避けるため、`tools/ffmpeg/ffmpeg.exe`・`ffprobe.exe`として同梱する方針（[SPEC.md](SPEC.md)参照）。
入手方法についてユーザーと相談中：
- 候補ソース: gyan.dev（ffmpeg.org公式が案内するWindowsビルド配布元の一つ）や BtbN/FFmpeg-Builds（GitHub Releases）
- ライセンス面: 再配布するなら GPL版よりLGPL版（"shared"ビルド）の方が安全という考慮点がある
- ダウンロードはClaude Codeの安全ルール上「明示的な許可」が必要な操作のため、実行前に必ずユーザーに確認する

## 次のアクション

- FFmpegバイナリの入手方法をユーザーと確定する（Claude Codeが指定ソースからダウンロードするか、ユーザーが自分で入手してtools/ffmpegに配置するか）
- 実際にサンプル音声ファイルを使ってCore側のロジック（デコード→検出→カット）をエンドツーエンドで動作確認する
- ユーザー自身によるWPF画面の実機動作確認（ファイル選択・波形表示・検出結果確認・カット実行）
- 検出パラメータのチューニング（実際のラジオ録音での精度検証）
