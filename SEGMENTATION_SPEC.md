# 区切り検出の仕様（案）

2026-09-12 作成。ユーザー方針転換により、**「CMを検出してカットする」から「番組全体の区切りを見つける」へ目標を変更**したことに伴う再設計。
既存の検出ロジック（`BoundaryDetector` / `SegmentClassifier` / `RepeatSegmentDetector`）は本仕様では前提にせず、文献調査から組み直す。

## 1. 目標の再定義

**入力**: ラジオ番組の録音1本（2時間程度）、または同番組の複数日分。

**出力**: 番組全体を通した**区切り（boundary）の時刻リスト**と、区切りで分けられた各**区間のラベル**
（音楽／トーク／トーク＋BGM／無音）。CM判定は行わない。

区切りの対象:
- 曲 ↔ トーク
- 曲 ↔ 曲
- 番組 ↔ CM
- ジングルの前後、コーナーの切り替わり

**カットするかどうかはユーザーが画面で判断する。** 本機能はその判断材料を提示することに徹する。

### なぜ自動判定を目標にしないのか（調査で判明した性能上限）

broadcast音声の音楽検出は、**学習済みのCNNを使っても頭打ちになる**ことが公開データセットで測定されている。

OpenBMAT（TV放送27.4時間・1647ファイル・4カ国、3名が全体を相互アノテーション）での実測値:

| | 音楽/非音楽の2値 | 前景/背景/無音楽の3値 |
|---|---|---|
| CNN（MMG, MIREX 2018優勝） | 正解率 **88.95%** | 正解率 **82.71%** |
| 人間のアノテータ間の完全一致 | **94.78%** | **89.1%** |

出典: Meléndez-Catalán, Molina & Gómez, OpenBMAT (TISMIR, DOI 10.5334/tismir.29)。本プロジェクトで内容を直接確認済み。

**含意**: 学習済みモデルでも89%、人間同士でも94.78%しか一致しない問題である。
本プロジェクトは**モデルを使わない**方針なので、これ以上の精度は原理的に望めない。
したがって「自動で正しく切る」ことを目標に据えるのは誤りで、
**人が確認する前提で、確認しやすい候補を出す**という設計が正しい。今回の目標変更はこの点で妥当。

### 最も難しいケース（正直な限界）

**DJがBGMの上で喋っている区間**（トーク＋音楽の同時進行）が最難。
Scheirer & Slaney (ICASSP 1997) は、音楽/音声の2値では誤り5.3〜6.0%（2.4秒窓で統合すると1.4%）を達成する一方、
**「音楽と音声の同時」を含む3値分類では約65%**と報告している。
（※この65%という数値は調査エージェントによる全文読解に基づく。本プロジェクト環境ではPDFがデコードできず直接の再確認ができていない。
上記OpenBMATのCNNでも前景音楽の再現率69.96%・背景音楽76.09%と低く、同じ傾向を示している。）

**設計方針**: この区間は「判定不能」として明示的に低確信度で提示する。無理に音楽かトークかを決めない。

## 2. 到達可能な精度の目安（評価の基準値）

区切り検出の精度は、**必ず自明なベースラインと比較する**こと。比較しないと改善したように錯覚する。

- 「3秒ごとに機械的に区切る」だけで F ≈ 0.50〜0.58（Beatles / RWC-Pop、許容3秒）
- 教師なしの楽曲構造境界検出の実力: SALAMI で F@3秒 ≈ 0.38〜0.58（McFee & Ellis, ISMIR 2014）
- 良条件（Beatles/RWC）での最高水準: F@3秒 ≈ 0.75〜0.80、人間の上限 ≈ 0.90（Serrà et al. 2012/2014）
- 放送ニュースの話者・環境変化点検出: F ≈ 89.4（許容0.5秒、BIC・全共分散）（Cettolo et al. 2005）

## 3. 処理の全体像

```
[A] 特徴抽出（16kHzへ変更）
     ↓
[G] Footeのチェッカーボード新規性 → 区切り候補の生成（既存実装を流用）
     ↓
[B] 時間ラグ行列による「繰り返し拒否」← 最重要。曲の途中での誤分割を消す
     ↓
[C] 音楽/トークのラベル付け（最小継続長つきViterbi）→ 同ラベルの隣接区間を統合
     ↓
[F] 音量の落ち込みで区切り位置を微調整（既存の LoudnessBoundaryRefiner）
     ↓
[D] 評価ハーネス（ユーザー注釈との突き合わせ）
```

## 4. 各段階の仕様

### [A] 特徴抽出（前提・難易度: 低）

**解析用のデコードを 8kHz → 16kHz に変更する。**
スペクトルロールオフ・ゼロ交差率・スペクトル変化量は 4kHz 以上の成分に強く依存しており、
8kHz（ナイキスト4kHz）ではこれらの識別力を捨てていることになる。

- 25ms窓 / 10msホップで自前FFT → パワースペクトル
- 10msフレームごと: RMS、ゼロ交差率、スペクトル重心、**95パーセンタイルのロールオフ**、スペクトル変化量、対数帯域エネルギー約24本
- **0.5秒のテクスチャフレーム**に集約し、各特徴の**平均と分散**を保持（現在のフレームレートは維持）
- 追加: **4Hz変調エネルギー**（各帯域の包絡を4Hz中心の2次バンドパスに通し、二乗・平滑化し、その帯域の総エネルギーで正規化して総和）
- 追加: **低エネルギーフレーム率**（RMSが直近1秒平均の50%未満のフレームの割合）

出典: Scheirer & Slaney (ICASSP 1997)。すべて純粋なC#で実装可能。

### [B] 時間ラグ行列による繰り返し拒否（最重要・難易度: 中〜高）

**これが「1曲の途中で切れてしまう」という現行の最大の失敗に対する唯一の解**。

**重要な注意**: 楽曲構造解析の論文（Serrà et al. 2012/2014、McFee & Ellis 2014）は
**イントロ→Aメロ→サビの境界を「見つける」ことを目的**としている。つまり我々が消したい境界こそが彼らの正解である。
そのまま導入してはいけない。**繰り返し情報を「拒否条件」として使う**のが正しい使い方。

原理: 時間ラグ行列 `L(ℓ, n) = S(n+ℓ, n)` では、自己相似行列の斜めの繰り返し線が**水平線**になる。
- 1曲の中の Aメロ→サビ→Aメロ という構造では、内部の境界は**あるラグ ℓ の水平線が張る領域の内側**に入る
- 曲→トークのような本当の項目境界は、**どのラグにも水平線が横切らない**

したがって「**十分に長い繰り返しの水平線が跨いでいる候補は区切りにしない**」という拒否規則が立つ。

実装:
1. テクスチャ特徴を遅延埋め込み（τ=1、履歴 m ≈ 4秒）
2. **相互k近傍**による2値の再帰行列（κ = 0.04）
3. 巡回時間ラグ行列に変換
4. 分離可能なガウス核で平滑化（σ² = 0.16、ラグ方向 ≈ 0.3秒、時間方向 25〜30秒）
5. **20秒以上の水平線**を検出し、それが跨ぐ区切り候補を抑制する

計算量: N = 7200秒 / 0.5秒 = 14,400 → N² = 2.07億。再帰行列をビット行列で持てば **26MB**。
k近傍計算は約10^10回の積和で、`System.Numerics.Vector<float>` を使えばC#で数十秒。
**固有値分解も行列ライブラリも不要。**

出典: 構成とパラメータは Serrà et al. (AAAI 2012 / IEEE TMM 2014)、時間ラグによる繰り返しの考え方は Goto (IEEE TASLP 2006, RefraiD)。
**※「繰り返しが跨ぐ境界を拒否する」という規則そのものを述べた論文は見つかっていない**（調査エージェントによる合成）。
実データでの検証が必須。

**副産物**: 水平線の両端が**別ファイルにまたがる**場合、それは「別の日にも同じ音が流れた」＝CMの強い証拠になる。
現在の繰り返し検出の役割をそのまま引き継げる。

### [C] 音楽/トークのラベル付けと同ラベル統合（難易度: 中）

**トーク＋BGMでの過剰分割を最終的に潰すのはこの段階**。新規性検出を生き延びた分割も、
「ラベルが変わっていない」という条件では生き残れない。

1. 0.5秒フレームごとに、Scheirer & Slaney の**最良の3特徴**（4Hz変調エネルギー、スペクトル変化量の分散、パルス指標）
   ＋低エネルギーフレーム率でスコア化（この3つで全13特徴とほぼ同等の性能）
2. 学習データが無いので、**対数変換した特徴に対しファイルごとにEMで2成分の対角ガウス混合を当てはめる**（教師なし、約60行）
3. **最小継続長を明示した2〜4状態のHMMをViterbiで平滑化**（最小継続長 ≈ 8秒）
4. **隣接する同ラベル区間を統合する**
5. トーク＋音楽の同時区間は「判定不能」として低確信度で提示し、統合も分割も自動では行わない

出典: 特徴は Scheirer & Slaney (1997)、最小継続長つきHMM平滑化は Ajmera, McCowan & Bourlard (Speech Communication 2003)。
※Ajmeraらの特徴量そのものは学習済みMLPを使うため対象外。**平滑化の枠組みだけ**を借りる。

### [D] 評価ハーネス（難易度: 低・効果: 極大）

**これが無いと、本当の改善と「たまたまこのファイルに合った閾値」を区別できない。**

- ユーザーが確認した区切りを単純な時刻リストとして保存する
- **許容0.5秒と3秒の両方**で境界の適合率・再現率・F値を算出
- **中央値ずれ**（正解→推定、推定→正解の両方向）を算出
- **必ず3つのベースラインを併記**: 平均区間長ごとに区切る／N個をランダムに置く／3秒ごとに区切る

出典: mir_eval (Raffel et al., ISMIR 2014)、評価指標は Turnbull et al. (ISMIR 2007)。

### [E] ΔBICによる変化点検出（第2の候補生成器・難易度: 中・優先度低）

**ファイルごとのパーセンタイルを、コーパス全体で1つ決めるλに置き換えられる**のが利点。

```
ΔBIC_i = (N/2)·log|Σ| − (i/2)·log|Σ₁| − ((N−i)/2)·log|Σ₂| − λ·P
P      = ((d + d(d+1)/2) / 2) · log N
```

`i_max = argmax ΔBIC_i` で `ΔBIC > 0` なら変化点。窓を粗い刻みで評価しながら成長させ、
検出したら細かい刻みで再評価して確認する。**全共分散を使うこと**（対角にすると F が 5.9 落ちる）。
λ = 1.0 から始めてユーザー注釈で較正（放送ニュースでの最適値は 0.85）。

出典: Cettolo, Vescovi & Rizzi (Computer Speech & Language 19(2), 2005)。
**大域的な動的計画法版は実装しないこと**（+0.4 F のために100倍の時間がかかると測定されている）。

### [F] 音量の落ち込みによる位置の微調整（難易度: 低・効果: 小）

窓RMSが30秒移動中央値の15%未満に200ms以上落ちる箇所を検出し、
**既に採用された区切りの位置をそこへ吸着させるだけ**に使う。現在の `LoudnessBoundaryRefiner` の役割は正しい。

**やらないこと**: BS.1770 / EBU R128 のラウドネスメーターは実装しない。
(a) 8〜16kHzではK特性の高域シェルフが意味を持たない、
(b) R128準拠の放送局はラウドネスを正規化しているため「CMは音が大きい」という手がかりは既に消えている。

### [G] Footeのチェッカーボード新規性（実装済み・維持）

現行実装はユーザー確認済みの正解3点を**誤差0.0秒**で検出できており、これは期待できる最良の水準。
カーネル幅は8秒（L ≈ 16秒）で適正域にある。**この段階は後退させない。**

出典: Foote (ICME 2000)。

## 5. 実装順序

| 順 | 段階 | 理由 | 難易度 |
|---|---|---|---|
| 1 | [D] 評価ハーネス | これが無いと以降の改善を測定できない | 低 |
| 2 | [A] 16kHz化＋特徴追加 | 以降すべての前提 | 低 |
| 3 | [B] 繰り返し拒否 | 最大の失敗（曲の途中での分割）への唯一の解 | 中〜高 |
| 4 | [C] ラベル付けと同ラベル統合 | トーク＋BGMの過剰分割を潰す | 中 |
| 5 | [E] ΔBIC | 閾値をファイル依存から解放する | 中 |
| 6 | [F] 位置の微調整 | 既存実装で概ね足りている | 低 |

**[D]を最初に作ること。** 現状は「実データで動かして目視で良さそう」を繰り返しており、
定量的な比較ができていない。ユーザーの確認済み注釈（現時点で12.82秒・80.14秒・378.26秒の3点、
および「1:20-6:18は1曲」「6:18-16:10はCMなし」といった区間情報）を機械可読な形にすることが先決。

## 6. 採用しない手法と理由

- **McFee & Ellis のスペクトラルクラスタリング** (ISMIR 2014): N=14,400のラプラシアンの固有ベクトルが必要。
  密行列で約830MB・3×10^12 flops。疎行列のLanczos法が要るが数値計算として重い割に、
  SALAMIでの実力は F@3秒 ≈ 0.46〜0.58 に留まる。[B]で同等の繰り返し情報が遥かに安く得られる。
- **学習済みモデル全般**: プロジェクト方針（モデル非依存・完全ローカル・Android移植の可能性）による。
- **音楽/トーク判別を「カットするか」の門番にすること**: 以前の方針で却下済み。
  今回は**区切りを見つけるため**に使うのであって、カットの可否を決めるためではない。この区別を維持する。

## 7. 主要参考文献

本プロジェクトで内容を確認した文献:

- J. Foote, "Automatic Audio Segmentation Using a Measure of Audio Novelty", ICME 2000, pp. 452–455.
- E. Scheirer & M. Slaney, "Construction and Evaluation of a Robust Multifeature Speech/Music Discriminator", ICASSP 1997.
  https://www.ee.columbia.edu/~dpwe/papers/ScheiS97-mussp.pdf
- J. Serrà, M. Müller, P. Grosche & J. Ll. Arcos, "Unsupervised Detection of Music Boundaries by Time Series Structure Features", AAAI 2012, pp. 1613–1619.
  拡張版: IEEE Trans. Multimedia 16(5):1229–1240, 2014. DOI 10.1109/TMM.2014.2310701
- B. McFee & D. Ellis, "Analyzing Song Structure with Spectral Clustering", ISMIR 2014, pp. 405–410.
- M. Cettolo, M. Vescovi & R. Rizzi, "Evaluation of BIC-based algorithms for audio segmentation", Computer Speech & Language 19(2):147–170, 2005.
- C. Raffel et al., "mir_eval: A Transparent Implementation of Common MIR Metrics", ISMIR 2014, pp. 367–372.
- B. Meléndez-Catalán, E. Molina & E. Gómez, "Open Broadcast Media Audio from TV (OpenBMAT)", TISMIR. DOI 10.5334/tismir.29
  （数値を本プロジェクトで直接確認済み）
- M. Goto, "A chorus section detection method for musical audio signals and its application to a music listening station", IEEE TASLP 14(5), 2006.
  （PDFが画像スキャンのため内部の式は未確認）
- J. Ajmera, I. McCowan & H. Bourlard, "Speech/music segmentation using entropy and dynamism features in a HMM classification framework", Speech Communication 40(3):351–363, 2003.
- S. Dixon, "Onset Detection Revisited", DAFx-06, pp. 133–137.（現行のピーク選出の出典）
