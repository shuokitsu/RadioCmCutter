# ラジオCM自動カットアプリ 仕様書（ドラフト）

作成日: 2026-09-11
状態: **検討中ドラフト**（未確定事項あり、下記「保留事項」参照）

## 1. 概要

ラジオ番組の録音ファイルからCM区間を自動検出し、カット（除去）するWindows向けローカルアプリケーション。
外部API・ネット通信を一切使わず、配布したzipを解凍してexeを実行するだけで動作することを目指す。

## 2. 配布・実行環境

- 配布形態: zipファイル。解凍後、exeをダブルクリックで起動（インストーラー不要）
- 外部ネットワーク通信: 一切なし（API呼び出し、アップデートチェック等も含めない）
- 依存ライブラリ: 同梱可能なものは同梱し、別PCでの追加インストール作業（FFmpegの別途インストール等）を発生させない
- 実行環境: Windows（将来的にAndroid化を検討中のため、コアロジックは特定OSに依存しない設計を意識する）

## 3. 対応ファイル形式

- 必須: `mp3`, `aac`, `m4a`
- 追加（対応容易なら）: `wav` など。デコード/エンコードはFFmpeg（同梱バイナリ）に委譲するため、FFmpegが対応する形式は原則追加コストが低い

## 4. 動作モード

### 4.1 カット＋自動再結合モード
CM区間を検出して除去し、残りの区間を1つの音声ファイルとして再結合・出力する。

### 4.2 カットのみモード（動作確認用）
検出したCM区間ごとにファイルを分割し、結合はしない。検出精度の確認や、誤検出時の見直し用途を想定。

## 5. 実行方式

- 単一ファイル指定モード: ファイルを1つ選択して実行
- ディレクトリ指定モード: フォルダを指定し、対応拡張子のファイルをまとめて連続処理（バッチ処理）
  - 同一フォルダ内に同番組の複数日分の録音がある場合、後述の「繰り返し検出」の精度向上にも寄与する

## 6. CM検出アルゴリズム

完全な自動検出は精度に限界があるため、複数手法を組み合わせたハイブリッド方式とする。
**方針: ①繰り返しセグメント検出を主軸、②音量・ラウドネス境界検出を境界の精緻化・補助として使用**（ユーザー確認済み）

### 6.1 ①繰り返しセグメント検出（メイン）
- 同一CM音源は同一番組内で複数回、または別日の録音間でも繰り返し使用される傾向がある、という仮定に基づく
- 処理概要:
  1. 音声を一定間隔（例: 0.5〜1秒単位）のチャンクに分割し、各チャンクの音響特徴量（MFCC等）を抽出
  2. ファイル内、および（ディレクトリ一括処理時は）ファイル間でチャンク同士の類似度を計算し、高い類似度で繰り返し出現する区間を検出
  3. 一定回数以上・一定長以上繰り返される区間をCM候補としてマーキング
- ディレクトリ一括処理と相性が良く、処理対象が増えるほど精度が向上する

### 6.2 ②音量・ラウドネス境界検出（補助）
- CM区間は音楽・ナレーションのミックスでコンプレッサーがかかり、音量が均一で大きい傾向があるという特徴を利用
- ①で検出した候補区間の開始・終了位置を、RMS/ラウドネスの変化点や無音区間を基準に精緻化する
- 単独でのCM/番組本編の判別には使わず、境界のズレ補正用途に限定する

### 6.3 将来検討（今回は見送り／保留）
- ジングル・シグナル音のテンプレートマッチング（番組ごとの手動登録が必要）
- ローカルSTT/LLMによるCM特有フレーズのキーワード検出 → **保留**（詳細はユーザーと別途相談予定。将来的なAndroid化との相性を含めて検討）

## 7. GUI仕様

- 検出結果確認画面: 波形表示＋検出区間のハイライト表示（ユーザー確認済み・推奨案採用）
  - CM候補区間を波形上に色分け表示
  - 各区間ごとにカット対象のON/OFFを切り替え可能（誤検出のスキップ、見送り含む）
  - 確認・調整後に最終的なカット実行を行う
  - 「カットのみモード」の結果もこの画面で確認できるようにする

## 8. 保留事項（別途相談予定）

- **実装言語・フレームワーク**: 開発環境の準備・動作確認の難易度も含めて別途相談
  - 候補: C#/.NET（将来のAndroid化をMAUIで見据えやすい・exe配布が容易）／Python（音声解析ライブラリが豊富）
- **ローカルLLM/STTの併用可否**: キーワード検出による精度向上効果と、Android化への影響を含めて別途詳しく説明・相談

## 9. 未着手の細部（実装フェーズで詰める）

- CM候補区間の最小長・最大長の閾値、繰り返し判定の類似度閾値などのパラメータ
- 誤検出時の学習・調整機能の有無（ユーザーによる手動補正結果を次回以降に活かすか）
- 出力ファイルの命名規則、出力先ディレクトリ構成
- 処理ログ・レポート（検出区間の時刻リストをテキスト等で出力するか）
